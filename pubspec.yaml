name: flutte_atten
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  csv: ^6.0.0
  device_preview: ^1.2.0
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
